import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      // Magic Mint
      main: '#A3E6C5',
    },
    secondary: {
      // Violet Blue Crayola
      main: '#7E6ECB',
      light: '#BCB4E4',
    },
  },
});

export default theme;
